Goal -> Generate random password for user .

User Input ->
    length .
    Should contain Upercase
    Should contain special character.
    Should contain Digits

Steps ->
make a list or array of input - upercasee letter, special character, digit, lowercase

randomly pick character upto the length

ensure we have at least one of each type

ensure length is valid